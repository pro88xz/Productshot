/**
 * TEMPORARY: minimal Database type to satisfy the Supabase clients.
 *
 * This will be replaced by auto-generated types from the Supabase CLI
 * once we install it. For now, we use a permissive type so TypeScript
 * compiles cleanly.
 *
 * To regenerate: npx supabase gen types typescript --project-id <id>
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
